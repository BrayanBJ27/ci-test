name: Hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-18.04
    steps:
      - name: Echo the messages
        run: echo "Hello guys"
      - name: Tell me the date
        run: date
      - run: ls -l
